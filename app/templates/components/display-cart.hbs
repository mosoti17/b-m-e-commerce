{{#each shopingCart.items as |item|}}
<div class="row">
  <div class="col-md-3">
    <h2>{{item.name}}</h2>
    <img src={{item.image}}>
    <p>{{item.price}}</p>
    <button {{ action 'removeItem' item}}>Remove</button>
  </div>

</div>

{{/each}}
